{
  "name": "__themeName__",
  "author": "__authorName__",
  "version": "0.0.0",
  "description": "__themeDesc__",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "dev": "node scripts/build ./ --client all --watch --plugins \"[ 'postcss/generic-classes({keepOriginal: true})' ]\"",
    "build": "node scripts/build ./ --client all",
    "build:all": "node scripts/build ./ --client all",
    "build:bd": "node scripts/build ./ --client bd",
    "build:stylus": "node scripts/build ./ --client stylus",
    "bump": "node scripts/bump patch",
    "bump:major": "node scripts/bump major",
    "bump:minor": "node scripts/bump minor",
    "bump:patch": "node scripts/bump patch",
    "bump:alpha": "node scripts/bump alpha"
  },
  "keywords": [],
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "chokidar": "^3.5.3",
    "cssnano": "^5.1.14",
    "fast-glob": "^3.2.12",
    "postcss": "^8.4.19",
    "sass": "^1.54.4",
    "tinycolor2": "^1.4.2"
  },
  "optionalDependencies": {
    "readline-sync": "^1.4.10"
  }
}
