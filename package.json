{
  "name": "__themeName__",
  "author": "__authorName__",
  "version": "0.0.0",
  "description": "",
  "private": true,
  "main": "./scripts/build/index.js",
  "scripts": {
    "dev": "node scripts/build ./ -c all --watch",
    "build:all": "node scripts/build ./ -c all",
    "build:bd": "node scripts/build ./ -c betterdiscord",
    "build:stylus": "node scripts/build ./ -c stylus",
    "update-classes": "git submodule update --init --remote DiscordClassChanges && python3 scripts/update-classes.py src/"
  },
  "keywords": [],
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "@ctrl/tinycolor": "^3.4.1",
    "chokidar": "^3.5.3",
    "sass": "^1.54.4"
  }
}
